{{- if .Values.autoscaling.vpa.enabled }}
# Vertical Pod Autoscaler - Hybrid with HPA
# Note: Use HPA for scaling replicas, VPA for resource recommendations
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: {{ include "springboot-feature-flag.fullname" . }}-vpa
  labels:
    {{- include "springboot-feature-flag.labels" . | nindent 4 }}
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "springboot-feature-flag.fullname" . }}
  updatePolicy:
    updateMode: {{ .Values.autoscaling.vpa.updateMode }}
  resourcePolicy:
    containerPolicies:
      - containerName: {{ .Chart.Name }}
        minAllowed:
          cpu: {{ .Values.autoscaling.vpa.minCpu }}
          memory: {{ .Values.autoscaling.vpa.minMemory }}
        maxAllowed:
          cpu: {{ .Values.autoscaling.vpa.maxCpu }}
          memory: {{ .Values.autoscaling.vpa.maxMemory }}
        controlledResources: ["cpu", "memory"]
{{- end }}
