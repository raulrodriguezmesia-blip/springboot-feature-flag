name: Subir a OneDrive

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  subir-onedrive:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Autenticar en Azure
        uses: azure/login@v3
        with:
          creds: '{"clientId":"${{ secrets.AZURE_CLIENT_ID }}","clientSecret":"${{ secrets.AZURE_CLIENT_SECRET }}","subscriptionId":"${{ secrets.AZURE_SUBSCRIPTION_ID }}","tenantId":"${{ secrets.AZURE_TENANT_ID }}"}'

      - name: Subir archivos a OneDrive
        run: |
          echo "Subiendo archivos a OneDrive..."
          # az storage blob upload-batch --account-name $STORAGE_ACCOUNT --source ./reports --destination $CONTAINER
